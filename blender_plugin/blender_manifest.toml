schema_version = "1.0.0"

id = "sketchup_link"
version = "0.28.0"
name = "SketchUp Link & Importer"
tagline = "Live sync from SketchUp to Blender + native .skp file import"
maintainer = "Jason Kraus"
type = "add-on"

blender_version_min = "4.2.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

[permissions]
files = "Read .skp files from disk and export to disk"
network = "Connect to sketchup-link Unix socket for live sync"
